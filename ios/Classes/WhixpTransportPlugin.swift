import Flutter

/// Stub plugin so Flutter recognizes the platform. Native lib is loaded via Dart FFI.
public class WhixpTransportPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {}
}

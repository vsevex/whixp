name: jid
description: A starting point for Dart libraries or applications.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.1.0

dependencies:
  escaper:
    path: ../escaper
  lint: ^2.1.2

dev_dependencies:
  test: ^1.21.0

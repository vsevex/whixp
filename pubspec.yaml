name: echox
description: Streamline Your XMPP Messaging with Lightweight Dart-Based XMPP Client
version: 0.1.0+3
repository: https://github.com/vsevex/echox
issue_tracker: https://github.com/vsevex/echox/issues
topics: [xmpp, im, network, websocket]

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  crypto: ^3.0.3
  dartz: ^0.10.1
  events_emitter: ^0.5.2
  logger: ^2.0.2
  memoize: ^3.0.0
  unorm_dart: ^0.2.0
  web_socket_client: ^0.1.0
  xml: ^6.3.0
  xpath_selector_xml_parser: ^3.0.1

dev_dependencies:
  convert: ^3.1.1
  lint: ^2.1.2
  test: ^1.21.0

platforms:
  android:
  ios:
